# All regenerable build output lives under build/, generated by build.sh: the verified
# downloads (build/dl), the scratch work tree (build/work), and the final images
# (rootfs.ubifs / rootfs.ubi). Nothing outside build/ is generated, so this one rule is
# the complete ignore set for this tree.
build/
